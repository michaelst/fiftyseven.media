use Mix.Config

config :fiftysevenmedia, FSMWeb.Endpoint,
  http: [port: 4000],
  debug_errors: true,
  code_reloader: true,
  check_origin: false,
  watchers: []

config :fiftysevenmedia, FSMWeb.Endpoint,
  live_reload: [
    patterns: [
      ~r{priv/static/.*(js|css|png|jpeg|jpg|gif|svg)$},
      ~r{priv/gettext/.*(po)$},
      ~r{lib/api_web/views/.*(ex)$},
      ~r{lib/api_web/templates/.*(eex)$}
    ]
  ]

config :logger, :console, format: "[$level] $message\n"

config :phoenix, :stacktrace_depth, 20

config :fiftysevenmedia, FSM.Repo, database: "fifty_seven_media_dev"
